# ✨ 我的超可爱个人主页 (Kawaii Homepage)

这是一个专为可爱女生设计的个人主页模板！你可以展示你的头像、心情和爱好，还有一个专属的编辑器来修改内容。不需要写代码，按照这个说明书一步步操作就可以啦！

## 📂 项目结构说明

这个文件夹里都有些什么呢？

```
kawaii-homepage/
├── index.html          # [对外展示页] 给朋友们看的页面，最好看的一页！
├── editor.html         # [秘密编辑页] 你自己用的，可以修改名字、心情等
├── data/
│   └── profile.json    # [核心数据] 你的所有信息都存在这里
├── assets/             # [素材库]
│   └── css/            # 样式文件，控制页面长什么样
├── nginx.conf.example  # [服务器配置] 部署到服务器时用的参考文件
└── README.md           # [说明书] 就是你现在看到的这个文件
```

---

## 🚀 第一步：在本地运行（试试看！）

在你把网站发布到互联网之前，先在自己的电脑上玩一玩。

1.  **打开方式**：
    *   **推荐**：如果你安装了 VS Code，安装 "Live Server" 插件，右键 `index.html` 选择 "Open with Live Server"。
    *   **简单**：直接双击 `index.html` 打开可能无法读取数据（因为浏览器安全限制）。
    *   **最简单的方法**：把整个文件夹拖到浏览器里试试，或者安装一个简单的 HTTP 服务器。

2.  **如何修改内容**：
    *   用浏览器打开 `editor.html`。
    *   在左边修改你的昵称、签名、添加爱好。
    *   右边会实时显示修改后的样子。
    *   修改满意后，点击最下方的 **"保存配置 (profile.json)"** 按钮。
    *   浏览器会下载一个 `profile.json` 文件。
    *   **关键步骤**：把下载下来的文件，覆盖掉项目文件夹里 `data/profile.json` 这个文件。

---

## ☁️ 第二步：部署到服务器（让所有人都能看到）

这一步稍微有点难，但是跟着做一定没问题！假设你已经买好了 **腾讯云轻量应用服务器 (Ubuntu)**。

### 1. 准备 GitHub 仓库

首先，我们要把代码上传到 GitHub（代码托管平台）。

1.  注册并登录 [GitHub](https://github.com/)。
2.  点击右上角 `+` -> `New repository`。
3.  Repository name 填 `my-kawaii-home`，点击 `Create repository`。
4.  在你的电脑上（项目文件夹下）打开终端（Terminal）或 Git Bash，输入以下命令：

```bash
# 初始化 Git
git init

# 把所有文件加入暂存区
git add .

# 提交第一次修改
git commit -m "我的第一次提交！"

# 关联到你的 GitHub 仓库 (把下面的链接换成你自己的!)
git remote add origin https://github.com/你的用户名/my-kawaii-home.git

# 推送到 GitHub
git push -u origin master
```

### 2. 服务器设置

登录腾讯云控制台，找到你的服务器，点击 **"登录"** (或者使用 SSH)。

**在服务器终端输入以下命令：**

```bash
# 1. 更新系统软件
sudo apt update
sudo apt install git nginx -y

# 2. 进入网页目录
cd /var/www

# 3. 把你的代码拉下来 (把链接换成你自己的!)
sudo git clone https://github.com/你的用户名/my-kawaii-home.git kawaii-homepage

# 4. 配置 Nginx (让别人能访问)
# 删除默认配置
sudo rm /etc/nginx/sites-enabled/default
# 创建新配置
sudo nano /etc/nginx/sites-available/kawaii
```

**在打开的编辑器里，复制粘贴 `nginx.conf.example` 里的内容。**
*   按 `Ctrl + O` 然后 `Enter` 保存。
*   按 `Ctrl + X` 退出。

```bash
# 5. 启用配置并重启 Nginx
sudo ln -s /etc/nginx/sites-available/kawaii /etc/nginx/sites-enabled/
sudo nginx -t   # 检查配置有没有写错，如果显示 successful 就对了
sudo systemctl restart nginx
```

🎉 **恭喜！** 现在在浏览器输入你的 **服务器公网 IP 地址**，就能看到你的网站啦！

---

## 🔄 第三步：如何更新网站？

以后你想更新心情或者换头像了，怎么办？

1.  **在本地电脑上**：打开 `editor.html`，修改内容，下载 `profile.json` 并覆盖。
2.  **提交到 GitHub**：
    ```bash
    git add .
    git commit -m "更新了心情"
    git push
    ```
3.  **在服务器上**（登录服务器终端）：
    ```bash
    cd /var/www/kawaii-homepage
    sudo git pull
    ```
    *只需要这一行命令，你的网站就自动更新啦！*

---

## 🎨 进阶玩法

*   **换背景**：把喜欢的图片放到 `assets/` 文件夹，修改 `style.css` 里的 `background-image`。
*   **改字体**：在 `style.css` 里引入你喜欢的 Google Fonts。

有问题随时问我哦！✨
